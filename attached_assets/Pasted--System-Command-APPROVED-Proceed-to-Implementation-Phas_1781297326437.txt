[System Command: APPROVED. Proceed to Implementation - Phase 2]

Context: Phase 1 is perfect. The `fastembed` ONNX pivot is brilliant for our 16GB CPU constraint. The gzip manifest handling is approved.

Task: Write the Python script for Phase 2 (`engine/phase2_ranker.py`).
Goal: Take the JD text, get Top 1000 candidates via FAISS, lazy-load their JSON data, apply Redrob's hard Honeypot filters and JD-specific mathematical multipliers, and extract the Top 100.

Requirements & Mathematical Logic:
1. FAISS Query: Embed the target JD text (using the same `fastembed` model). Query the FAISS index to retrieve the Top 1000 candidate IDs and their base Cosine Similarity scores.
2. Lazy Loading: Use `manifest.json` and `byte_offset_index.json` to `seek()` and load ONLY the JSON records for these 1000 candidates into memory.
3. The Honeypot Filter (Immediate Drop to Tier 0 / Score 0):
   - Overlapping Timelines: Disqualify if total experience years > age of their current company.
   - The "Fake Expert": Disqualify if `skill_assessment_scores` contain "expert" level but actual experience is 0.
   - Title Mismatch Trap: Disqualify if `current_title` contains "Marketing", "Sales", or "HR" but skills list "RAG", "Pinecone", or other AI engineering keywords.
4. The NDCG@10 Multipliers (Modifying the FAISS Base Score):
   - The "Consulting Lifer" Penalty: Multiply score by 0.1 if 100% of their career history is strictly at TCS, Infosys, Wipro, Accenture, Cognizant, or Capgemini (No product company experience).
   - The "Job Hopper" Penalty: If (Total Experience / Number of Companies) < 1.5 years, multiply score by 0.5.
   - Notice Period Decay: If `notice_period_days` <= 30, multiplier is 1.0. For every additional 30 days, subtract 0.15 from the multiplier.
   - Engagement Boost: Multiply final score by `(1 + (recruiter_response_rate * interview_completion_rate))` to push highly active candidates to the absolute top.
5. Sort & Trim: Sort the candidates by their final modified score in descending order. Take the Top 100.
6. CSV Prep: Output a temporary structure with columns: `candidate_id`, `rank`, `score`. Stub out the `reasoning` column with a temporary string (e.g., "AST generation pending Phase 3").

Generate the clean, documented Python code. Run it against the mock data generated in Phase 1 to ensure it executes without errors.